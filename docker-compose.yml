version: "3.9"

services:
  app:
    build: .
    container_name: agentic-research-assistant

    ports:
      - "8501:8501"

    env_file:
      - .env

    volumes:
      - ./chroma_db:/app/chroma_db
      - ./uploads:/app/uploads

    restart: unless-stopped